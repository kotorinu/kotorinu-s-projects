# Postmortems

No production incident has been recorded for RIALA Planner v0. The implementation treats incomplete source reads, ambiguous identity, duplicate possibility, permission failure, approval mismatch, and unknown external results as stop conditions. Add a dated entry here when a real run reveals a correction to a rule, test, or adapter.

